---
- name: Run Create VHMC Script.
  hosts: host
  remote_user: root
  vars_files:
     - secret_vars.yaml  
  tasks:
     - name: Execute create vhmc script
       command: 'python /nfs/tools/vHMC_Creation.py -i {{ hmc_ip }} -r {{ hmc_release }} -U -u {{ w3user }} -p {{ w3pass }} --cloud 84'
       no_log: true
